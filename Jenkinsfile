pipeline {
  agent any
  stages {
    stage('Checkout code') {
      steps {
        checkout scm
      }
    }

    stage('Build') {
      steps {
        echo "aba ra xdeba"
      }
    }

  }
  environment {
    registry = 'itemo/practical_task_ci_cd'
  }
}
